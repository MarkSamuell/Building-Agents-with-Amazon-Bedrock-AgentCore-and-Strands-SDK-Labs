# Direct dependencies only, pinned to the versions resolved on 2026-08-27.
# This file is also what the container build installs, so anything imported by
# the agent must appear here -- an import that works locally and is missing here
# is the commonest way a first deployment fails.

# The AgentCore runtime wrapper: BedrockAgentCoreApp and @app.entrypoint.
bedrock-agentcore==1.22.0

# The agentcore CLI: configure / dev / deploy / invoke.
# Prints a deprecation notice pointing at the Node CLI (@aws/agentcore); the
# course teaches this one.
bedrock-agentcore-starter-toolkit==0.3.12

# The agent loop, model providers, and the @tool decorator.
strands-agents==1.53.0

# Community tools package: calculator, current_time, http_request and the rest.
strands-agents-tools==0.8.6
